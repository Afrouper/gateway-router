spring:
  application:
    name: gateway-locator
  cloud:
    consul:
      discovery:
        enabled: true
    service-registry:
      auto-registration:
        enabled: false
    gateway:
      discovery:
        locator:
          enabled: true

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: health,routes

logging:
  level:
    de.afrouper.gateway: debug
